# Context Sync

## Windows MSI

Tagged releases build a per-machine Windows MSI that installs `work-context-sync.exe` under `Program Files` and adds that install directory to the system PATH. Installing or uninstalling the MSI requires an elevated prompt.
